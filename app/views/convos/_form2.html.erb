<%= form_with(model: convo, local: true) do |form| %>
  <% if convo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(convo.errors.count, "error") %> prohibited this convo from being saved:</h2>

      <ul>
      <% convo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <% form.label :user_id %>
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>


  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :convo_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :convo_description %>
  </div>

  <div class="field">
    <% form.label :likes %>
    <%= form.hidden_field :likes, id: :convo_likes %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :convo_url %>
  </div>

  <div class="field">
    <% form.label :convo_id %>
    <%= form.hidden_field :convo_id, id: :convo_convo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
